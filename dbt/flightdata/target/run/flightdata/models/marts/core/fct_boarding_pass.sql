
  
    

  create  table "flightdb"."bookings"."fct_boarding_pass__dbt_tmp"
  
  
    as
  
  (
    -- with stg_tickets as(
--     SELECT *
--     FROM "flightdb"."bookings"."stg_tickets"
-- ),

-- dim_passengers as(
--     SELECT *
--     FROM "flightdb"."bookings"."dim_passengers"
-- ),

-- stg_ticket_flights as(
--     SELECT *
--     FROM "flightdb"."bookings"."stg_ticket_flights"
-- ),

-- stg_flights as(
--     SELECT *
--     FROM "flightdb"."bookings"."stg_flights"
-- )
  );
  